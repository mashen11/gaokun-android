# drm_hwcomposer 的 uevent 监听线程（热插拔 / vblank 通知走 netlink uevent）。
#
# 证据：2026-09-18 实机 dmesg，10 条
#   scontext=tcontext=u:r:hal_graphics_composer_default:s0
#   tclass=netlink_kobject_uevent_socket { read }   path="socket:[21923]"
#
# ⚠️ 只写观测到的 read：create / bind / setopt 从来没被拒过（socket 编号一直是
#    同一个 21923，说明它确实建起来了）。多给等于凭空放权 —— 与
#    hal_sensors_default 那条 qipcrtr_socket 同一个取舍（见该文件）。
# ⬜ 上机复查判据：enforcing 下如果 hwc 的热插拔/vblank 失灵，再按新的 denial 补。
allow hal_graphics_composer_default self:netlink_kobject_uevent_socket read;
