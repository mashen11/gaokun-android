# drm_hwcomposer 的 uevent 监听线程（热插拔 / vblank 通知走 netlink uevent）。
#
# 证据：2026-09-18 实机 dmesg，10 条
#   scontext=tcontext=u:r:hal_graphics_composer_default:s0
#   tclass=netlink_kobject_uevent_socket { read }   path="socket:[21923]"
#
# ⚠️★★ 2026-09-18 装机复查，这条规则【写漏了】，而漏的原因值得单独记：
#    第一版只写了 `read`，理由是"只写观测到的权限"—— 但那次观测用的 dmesg
#    **早期启动已经滚掉了**（最早一条在 uptime 8154 秒），而 create/bind 恰恰
#    只在开机那一瞬发生一次。换成完整启动历史之后，`read` 消失了（规则生效），
#    露出来的正是 `create` 与 `bind`。
#    ★ 教训：「只写观测到的」是对的，但**观测窗口必须覆盖整个生命周期** ——
#      滚掉的日志会静默地把"只发生一次"的权限藏起来，而那恰恰是启动类权限。
# ⬜ 仍然不给 setopt/write：没被拒过。
allow hal_graphics_composer_default self:netlink_kobject_uevent_socket { create bind read };
