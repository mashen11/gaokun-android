arm64: dts: gaokun3: 后摄闪光灯 —— PMIC（pmc8280c / PM8350C）闪光模块 1+4 路，替掉不亮的 GPIO93

**依据（#106 → #110）**：Windows 的 `\_SB.FLSH`（QCOM0627）`_CRS` 是空的（无 GPIO/I2C），驱动包
`qccamflash_ext8280.inf` 只有 `IrLedCurrentMilliampere=700` 一条注册表 ⇒ 闪光灯走 **PMIC 闪光模块**；
GPIO93（现役 DT 当成 GPIO LED 的那个）打脉冲实测不亮（用户看的）。`pmc8280c` 是 `qcom,pm8350c`，
主线 `pm8350c.dtsi` 里就有 `led-controller@ee00`（`qcom,pm8350c-flash-led`），驱动 `leds-qcom-flash`。

**接线是实测出来的，不是抄的**：v1 把 4 路各建一个 LED，内核 #19 上逐路 torch（用户看背面）：
单路 1 亮、单路 4 亮、2/3 不亮；合并对照 1+4 亮 / 2+3 不亮（2026-09-14 12:10，#110）。
⇒ 收成一个节点 `led-sources = <1>, <4>`。驱动 `leds-qcom-flash.c:727-735` 按 `led-sources` 个数
分配通道，≤ max_channels(4) 即可，两路并联是它的正常用法。

**电流上限是保守假设**（模组手册没有）：torch 合计 200 mA、flash 600 mA / 400 ms。
Windows 那条 `IrLedCurrentMilliampere=700` 是 IR LED 的，不是白光的，没拿来当依据。

**顺带删掉 GPIO93 那个 gpio-led 与它的 pinctrl 状态**：留着会在 `/sys/class/leds/` 下出现一个
永远不亮的 `white:flash`，与真闪光灯同名，HAL/应用按名字找会找错。

⚠️ 故意**没**在 ov13b10 节点上写 `flash-leds`：那会让 v4l2-async 再多等一个 subdev（#81/#106 的坑）。
需要 `LEDS_CLASS_FLASH=y LEDS_QCOM_FLASH=y`（kernel-config-android.sh 已断言）。⚠️ diff 由 `git diff` 生成。

diff --git a/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi b/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi
index 937eda9..b87f09e 100644
--- a/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi
+++ b/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi
@@ -22,7 +22,7 @@
 	leds {
 		compatible = "gpio-leds";
 
-		pinctrl-0 = <&cam_indicator_en>, <&camera_flash_en>;
+		pinctrl-0 = <&cam_indicator_en>;
 		pinctrl-names = "default";
 
 		privacy_led: privacy-led {
@@ -33,13 +33,10 @@
 			panic-indicator;
 		};
 
-		camera_flash: led {
-			function = LED_FUNCTION_FLASH;
-			color = <LED_COLOR_ID_WHITE>;
-			gpios = <&tlmm 93 GPIO_ACTIVE_HIGH>;
-			linux,default-trigger = "none";
-			default-state = "off";
-		};
+		/*
+		 * GPIO93 曾在这里被当成 flash LED（white:flash）——实测打脉冲不亮（#106），
+		 * 连带的 camera_flash_en pinctrl 状态一起删了。真闪光灯是下面 &pmc8280c 的 led-0。
+		 */
 	};
 
 	vreg_camf_1p2: regulator-camf-1p2 {
@@ -112,6 +109,29 @@
 	};
 };
 
+/*
+ * 闪光灯（patches/0036）：Windows 的 \_SB.FLSH 设备没有 GPIO/I2C 资源 ⇒ 走 PMIC 闪光模块
+ * （pmc8280c = PM8350C，主线 leds-qcom-flash）；GPIO93 直驱实测不亮（#106）。
+ * 2026-09-14 内核 #19 四路逐个 torch 实测（用户看背面）：LED 接在 **1 + 4 路**（#110）。
+ * ⚠️ 电流上限是【保守假设】不是模组手册值：torch 合计 200 mA、flash 600 mA / 400 ms。
+ * ⚠️ 故意不在 ov13b10 节点上写 flash-leds：那会让 v4l2-async 再多等一个 subdev。
+ */
+&pmc8280c {
+	pmc8280c_flash: led-controller@ee00 {
+		compatible = "qcom,pm8350c-flash-led", "qcom,spmi-flash-led";
+		reg = <0xee00>;
+
+		camera_flash_led: led-0 {
+			function = LED_FUNCTION_FLASH;
+			color = <LED_COLOR_ID_WHITE>;
+			led-sources = <1>, <4>;
+			led-max-microamp = <200000>;
+			flash-max-microamp = <600000>;
+			flash-max-timeout-us = <400000>;
+		};
+	};
+};
+
 &camss {
 	/*
 	 * CSIPHY0, CSIPHY1 1.2v = l4d, 0.88v = l6d
@@ -272,12 +292,6 @@
 		bias-disable;
 	};
 
-	camera_flash_en: camera-flash-en-state {
-		pins = "gpio93";
-		function = "gpio";
-		drive-strength = <2>;
-		bias-disable;
-	};
 
 	camf_1p2_reg_en: camf-1p2-reg-en-state {
 		pins = "gpio44";
