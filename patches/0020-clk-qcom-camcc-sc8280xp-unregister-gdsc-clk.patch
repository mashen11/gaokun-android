clk: qcom: camcc-sc8280xp: 不再把 CAMCC_GDSC_CLK 注册成时钟（上游 backport）

**上游原始提交**：`499b4cb6710f`（Brian Masney，2026-07-08），
进的是 `qcom-clk-for-7.3` —— **不在我们的基线 v7.2-rc2 里**，所以要 backport。

★ 为什么怀疑它是我们那个 camss 电源域缺陷的修复：**上游报告的告警与我们实测到的
是同一个 GDSC、同一个函数、同一条 WARN**：

```
上游：titan_top_gdsc status stuck at 'on'    WARNING: drivers/clk/qcom/gdsc.c:178 at gdsc_toggle_logic
我们：titan_top_gdsc status stuck at 'off'   WARNING: drivers/clk/qcom/gdsc.c:185 at gdsc_toggle_logic
```

方向相反（上游是关不掉，我们是开不起来），但机制同源：**GDSC 要靠
`CAMCC_GDSC_CLK` 才能翻转状态**。camcc 的 probe 用
`qcom_branch_set_clk_en(regmap, 0xc1e4)` 把它设成常开，
但它**同时又被注册成一个普通时钟**，于是 clk/pmdomain 的 sync_state
在关闭"未使用时钟"时会把它关掉。修法是干脆不注册它。

⚠️★ **这是一个待验证的假说，不是已证实的修复。** 本机实测的反证是：
`clk_summary` 里 `camcc_gdsc_clk` 现在显示 **hardware enable = Y**（还开着），
而 `state_synced` 已经是 1 —— 与"被 sync_state 关掉"对不上。
但本仓的规矩是**实机为准**，所以编出来上机测，而不是靠推理下结论。
测法见 docs/stage4-findings.md #83：开机后跑一次 camtest，等 GDSC 塌缩，再跑一次。

⚠️ 已排除的四条（别重查，见 #83 第六节）：camcc 处于 runtime-suspend、
某个时钟被关（clk_summary 141 行逐行相同）、`RETAIN_FF_ENABLE` 位、
MMCX 父域档位/息屏（屏强制常亮时 mmcx 全程 on/416，照样失败）。

--- 以下为上游提交原文（模块列表已略）---

With the introduction of sync_state support in the clk and pmdomain
subsystems, the following warning happens when the unused clocks are
shutdown in camcc-sc8280xp:

    titan_top_gdsc status stuck at 'on'
    WARNING: drivers/clk/qcom/gdsc.c:178 at gdsc_toggle_logic+0x14c/0x160
    ... (Lenovo ThinkPad X13s / 21BX0016US, kernel 7.1.0+)

Signed-off-by: Brian Masney <bmasney@redhat.com>

diff --git a/drivers/clk/qcom/camcc-sc8280xp.c b/drivers/clk/qcom/camcc-sc8280xp.c
index 18f5a3eb313e15..1de238a85ab9f6 100644
--- a/drivers/clk/qcom/camcc-sc8280xp.c
+++ b/drivers/clk/qcom/camcc-sc8280xp.c
@@ -1754,24 +1754,6 @@ static struct clk_branch camcc_csiphy3_clk = {
 	},
 };
 
-static struct clk_branch camcc_gdsc_clk = {
-	.halt_reg = 0xc1e4,
-	.halt_check = BRANCH_HALT,
-	.clkr = {
-		.enable_reg = 0xc1e4,
-		.enable_mask = BIT(0),
-		.hw.init = &(struct clk_init_data){
-			.name = "camcc_gdsc_clk",
-			.parent_hws = (const struct clk_hw*[]){
-				&camcc_xo_clk_src.clkr.hw,
-			},
-			.num_parents = 1,
-			.flags = CLK_SET_RATE_PARENT,
-			.ops = &clk_branch2_ops,
-		},
-	},
-};
-
 static struct clk_branch camcc_icp_ahb_clk = {
 	.halt_reg = 0xc0d8,
 	.halt_check = BRANCH_HALT,
@@ -2840,7 +2822,6 @@ static struct clk_regmap *camcc_sc8280xp_clocks[] = {
 	[CAMCC_CSIPHY2_CLK] = &camcc_csiphy2_clk.clkr,
 	[CAMCC_CSIPHY3_CLK] = &camcc_csiphy3_clk.clkr,
 	[CAMCC_FAST_AHB_CLK_SRC] = &camcc_fast_ahb_clk_src.clkr,
-	[CAMCC_GDSC_CLK] = &camcc_gdsc_clk.clkr,
 	[CAMCC_ICP_AHB_CLK] = &camcc_icp_ahb_clk.clkr,
 	[CAMCC_ICP_CLK] = &camcc_icp_clk.clkr,
 	[CAMCC_ICP_CLK_SRC] = &camcc_icp_clk_src.clkr,
