From: vahiru <nekovahiru@outlook.com>
Subject: [PATCH] arm64: dts: gaokun3: 后摄 rotation 180 经目视确认（去掉 FIXME）

2026-09-24 用户目视确认：设备树后摄 `rotation = <180>`（0032 原值）+ PR #6 的 HAL
（SENSOR_ORIENTATION = (360 - rotation) % 360 = 180）下，Aperture 后摄取景是正的。
核对过当时设备上的真实状态：/proc/device-tree/…/camera@36/rotation = 0x000000b4，
debug.gaokun3.camera.orientation.* 属性都没设 —— 用户看到的确实就是 180。

⚠️ 本补丁的前身（同号，2026-09-24 凌晨写、未发布）按 PR #6 描述里的"前 90 / 后 270"改过值，
**后摄那一半与这次目视结论相反**，已作废；前摄那一半没有目视证据（PR 文档第 7.2 节的照片
证据反而支持前摄 `<0>`），一并撤掉，前摄保持 `<0>` 待看。
只改注释 ⇒ 编出来的 dtb 与只打到 0048 的逐字节相同（sha 8b390878…）。

`rotation` 是【逆时针】安装角（Documentation/devicetree/bindings/media/
video-interface-devices.yaml），驱动经 v4l2_ctrl_new_fwnode_properties() 变成只读的
V4L2_CID_CAMERA_SENSOR_ROTATION，libcamera 读成 properties::Rotation。
---
--- a/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi
+++ b/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi
@@ -226,7 +226,9 @@
 		lens-focus = <&voice_coil_motor>;
 
 		orientation = <1>;
-		rotation = <180>; /* FIXME */
+		/* Counter-clockwise (video-interface-devices.yaml). Confirmed on
+		 * device 2026-09-24: preview upright, SENSOR_ORIENTATION 180. */
+		rotation = <180>;
 
 		port {
 			ov13b10_ep: endpoint {
