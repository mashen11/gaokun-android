From: Vahiru <nekovahiru@gmail.com>
Subject: [PATCH] arm64: dts: gaokun3: usb_0 用 UTMI 时钟当 pipe 时钟（USB2-only 口）

⚠️ 待验证（2026-09-23 写，未上机）。案卷 docs/stage4-findings.md #118 §7。

现象（v7.2-rc2 内核 #24，port0 插着一台能枚举我们的主机）：
把 a600000.usb 的 role 从 device 切到 host 再切回 device，两边都坏：

    xhci-hcd xhci-hcd.5.auto: Host halt failed, -110
    xhci-hcd xhci-hcd.5.auto: can't setup: -110
    xhci-hcd xhci-hcd.5.auto: probe with driver xhci-hcd failed with error -110
    ...（切回 device 后，每秒一次，直到重启）
    dwc3 a600000.usb: request ... was not queued to ep0out
    udc a600000.usb: failed to start g1: -524

这就是案卷 #27「拔插 USB 后 adb 不回来、只能重启」的真实机制：任何一次角色切换
（UCSI 在重新插线时就会切一次）都把控制器留在这个状态。重置 FunctionFS
（sys.usb.config none→adb，adbd 重写描述符）无效 ⇒ 不在 f_fs，在控制器。

-110 与 0012 里记的"QMP combo PHY 的 USB3 pipe 时钟被 DP 抢走，dwc3 软复位超时 -110"
同一个签名。0012 用 maximum-speed = "high-speed" 让 gadget 跳过 USB3，但控制器本身
仍然要求 pipe_clk —— 缺的是 dwc3-qcom 专门为此准备的开关：

    qcom,select-utmi-as-pipe-clk:
      If present, disable USB3 pipe_clk requirement. Used when dwc3 operates
      without SSPHY and only HS/FS/LS modes are supported.
                    （Documentation/devicetree/bindings/usb/qcom,dwc3.yaml:147）

本机 glue 是 qcom,sc8280xp-dwc3（dwc3-qcom-legacy.c:792 读这个属性，probe 时写
QSCRATCH_GENERAL_CFG 的 PIPE_UTMI_CLK_SEL | PIPE3_PHYSTATUS_SW）。
同样是 otg + USB2-only 的上游先例：milos-fairphone-fp6.dts:886-893。

验收判据（同一次开机、线不动）：
  1. echo host > .../a600000.usb-role-switch/role → xhci-hcd 绑上（有 driver 符号链接），无 -110；
  2. echo device → UDC 回到 configured，主机那头重新看到 adb；
  3. 以上来回三次都成立。
不成立就撤掉本补丁（git apply -R），结论记回 #118。
---
--- a/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3.dts
+++ b/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3.dts
@@ -1301,6 +1301,14 @@
 };
 
 &usb_0 {
+	/*
+	 * port0 is USB2-only (&usb_0_dwc3 maximum-speed), and the QMP combo PHY
+	 * pipe clock may be owned by DP. Without this the core still waits for
+	 * pipe_clk on every role switch: xhci "Host halt failed, -110" in host
+	 * mode, and the gadget fails to start (-524) after switching back.
+	 */
+	qcom,select-utmi-as-pipe-clk;
+
 	status = "okay";
 };
 
