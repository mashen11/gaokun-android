clk: qcom: camcc-sc8280xp: 给八个 GDSC 补上等待值（en_rest/en_few/clk_dis = 2/2/0xf）

**动机**：camss 电源域缺陷（docs/stage4-findings.md #83 → #104）的候选根因之一。
#104 的硬件级证据链把触发点钉在【掉电那一侧】：域里真跑过时钟之后再塌缩，
`CFG_GDSCR` 就少了 bit 19，随后上电 FSM 冻死；而没跑过时钟的塌缩无害。

**机制（从本地源码读出，不是记忆）**：
* `drivers/clk/qcom/gdsc.c:38-40` 的默认值是 `EN_REST_WAIT_VAL 0x2 / EN_FEW_WAIT_VAL 0x8 /
  CLK_DIS_WAIT_VAL 0x2`，`gdsc_init()`（同文件 :435-444）在驱动没给值时**无条件用这组
  覆盖硬件复位值**。上游维护者 Konrad Dybcio 在 lkml 上明说过这组是"destined for MSM8974"
  （回复 sc8180x 的 "Remove wait_val for Titan GDSC"，2026-03-13）。
* 本机 `#104` 转储里 `GDSCR=0x00282801` ⇒ `EN_REST=2 EN_FEW=8 CLK_DIS=2`，正是那组默认值。
* `CLK_DIS_WAIT` 是"关掉域内时钟后等多少个周期再拉 clamp/塌缩"。只有域里有时钟在跑时
  它才有意义 —— 这与"干净塌缩无害、流过之后塌缩必坏"的实测形状一致。
* sc8280xp 的 camcc 驱动**一个 GDSC 都没给等待值**，而同代且寄存器偏移**逐个相同**
  （`titan_top` 0xc1bc、`ife_0` 0xa004）的 `camcc-sm8150.c` / `camcc-sc8180x.c`，
  以及 sc7280 / sm6350 / sm8550 / sm8650 / sa8775p / x1e80100，全部写的是
  `.en_rest_wait_val = 0x2, .en_few_wait_val = 0x2, .clk_dis_wait_val = 0xf`
  （见 refs/linux-master/drivers/clk/qcom/camcc-sm8150.c:1887 起、camcc-sc8180x.c:2548 起）。
* 下游（msm-5.4 direwolf）`drivers/clk/qcom/gdsc-regulator.c:976-982` **只在 DT 给了
  `qcom,clk-dis-wait-val` 时才改**这个字段，否则保留硬件复位值 —— 所以下游从来没跑过 2/8/2。

**做法**：照 sm8150/sc8180x 给全部八个 GDSC 补 `2/2/0xf`。这是"还原硬件复位值"，
不是新发明的数。⚠️ 是否真是本机的复位值，靠 `patches/0028` 的 `gdsc-dbg/init_raw`
上机读出来核实（UEFI 不碰相机域，`gdsc_init()` 覆盖之前读到的就是复位值）。

**判据（预先声明）**：`camtest --stop 4 --noqbuf` 之后塌缩 → `echo on > power/control`
能上电（`pm_genpd_summary` 里 `titan_top_gdsc` 回到 `on`），且**连做 5 次**都成。
反向：如果 `init_raw` 读出的复位值就不是 2/2/0xf，这条补丁的依据就塌了，要按读到的值改。

⚠️ 本补丁的 diff 由 `git diff` 生成，不是手写 hunk 头。

diff --git a/drivers/clk/qcom/camcc-sc8280xp.c b/drivers/clk/qcom/camcc-sc8280xp.c
index 660d865..e4ec7e3 100644
--- a/drivers/clk/qcom/camcc-sc8280xp.c
+++ b/drivers/clk/qcom/camcc-sc8280xp.c
@@ -2711,6 +2711,9 @@ static struct gdsc titan_top_gdsc;
 
 static struct gdsc bps_gdsc = {
 	.gdscr = 0x7004,
+	.en_rest_wait_val = 0x2,
+	.en_few_wait_val = 0x2,
+	.clk_dis_wait_val = 0xf,
 	.pd = {
 		.name = "bps_gdsc",
 	},
@@ -2721,6 +2724,9 @@ static struct gdsc bps_gdsc = {
 
 static struct gdsc ife_0_gdsc = {
 	.gdscr = 0xa004,
+	.en_rest_wait_val = 0x2,
+	.en_few_wait_val = 0x2,
+	.clk_dis_wait_val = 0xf,
 	.pd = {
 		.name = "ife_0_gdsc",
 	},
@@ -2731,6 +2737,9 @@ static struct gdsc ife_0_gdsc = {
 
 static struct gdsc ife_1_gdsc = {
 	.gdscr = 0xb004,
+	.en_rest_wait_val = 0x2,
+	.en_few_wait_val = 0x2,
+	.clk_dis_wait_val = 0xf,
 	.pd = {
 		.name = "ife_1_gdsc",
 	},
@@ -2741,6 +2750,9 @@ static struct gdsc ife_1_gdsc = {
 
 static struct gdsc ife_2_gdsc = {
 	.gdscr = 0xf004,
+	.en_rest_wait_val = 0x2,
+	.en_few_wait_val = 0x2,
+	.clk_dis_wait_val = 0xf,
 	.pd = {
 		.name = "ife_2_gdsc",
 	},
@@ -2751,6 +2763,9 @@ static struct gdsc ife_2_gdsc = {
 
 static struct gdsc ife_3_gdsc = {
 	.gdscr = 0xf070,
+	.en_rest_wait_val = 0x2,
+	.en_few_wait_val = 0x2,
+	.clk_dis_wait_val = 0xf,
 	.pd = {
 		.name = "ife_3_gdsc",
 	},
@@ -2761,6 +2776,9 @@ static struct gdsc ife_3_gdsc = {
 
 static struct gdsc ipe_0_gdsc = {
 	.gdscr = 0x8004,
+	.en_rest_wait_val = 0x2,
+	.en_few_wait_val = 0x2,
+	.clk_dis_wait_val = 0xf,
 	.pd = {
 		.name = "ipe_0_gdsc",
 	},
@@ -2771,6 +2789,9 @@ static struct gdsc ipe_0_gdsc = {
 
 static struct gdsc ipe_1_gdsc = {
 	.gdscr = 0x9004,
+	.en_rest_wait_val = 0x2,
+	.en_few_wait_val = 0x2,
+	.clk_dis_wait_val = 0xf,
 	.pd = {
 		.name = "ipe_1_gdsc",
 	},
@@ -2781,6 +2802,9 @@ static struct gdsc ipe_1_gdsc = {
 
 static struct gdsc titan_top_gdsc = {
 	.gdscr = 0xc1bc,
+	.en_rest_wait_val = 0x2,
+	.en_few_wait_val = 0x2,
+	.clk_dis_wait_val = 0xf,
 	.pd = {
 		.name = "titan_top_gdsc",
 	},
