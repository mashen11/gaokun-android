clk: qcom: gdsc: 解绑时注销 genpd（上游 backport，修 #87 查到的 unbind 缺陷）

**上游原始提交**：`86b23609d5e1`（Herman van Hazendonk，2026-06-02）
—— **不在我们的基线 v7.2-rc2 里**（本机 gdsc.c 的 `gdsc_unregister()`
仍是旧版三行）。

★ 它修的正是 [#87](../docs/stage4-findings.md) 顺带查出的那条：
`gdsc_register()` 在 probe 里 `pm_genpd_init()` 注册的那些 genpd，
**unbind 时从来没有被注销**，于是 rebind 必然撞名：

```
debugfs: 'titan_top_gdsc' already exists in 'pm_genpd'
camcc-sc8280xp ad00000.clock-controller: probe with driver camcc-sc8280xp
                                          failed with error -22
```

后果不是"试不成"，而是**相机子系统进入"已解绑且绑不回去"的状态，只能重启**。
打上之后 `unbind`/`bind` camcc 才是一条可用的实验手段。

⚠️ **它不会修好电源域那个缺陷**：`gdsc_unregister()` 只在驱动解绑时跑，
相机测试全程碰不到它。放进同一个内核是因为它**与 0021 的判据零交叉**，
不破坏"单变量测试"这条规矩。

顺带：上游还调换了顺序，先摘 OF provider 再拆子域，避免
`of_genpd_get_from_provider()` 与拆除竞态时拿到一个正在移除的域。

diff --git a/drivers/clk/qcom/gdsc.c b/drivers/clk/qcom/gdsc.c
--- a/drivers/clk/qcom/gdsc.c
+++ b/drivers/clk/qcom/gdsc.c
@@ -678,10 +678,18 @@ int gdsc_register(struct gdsc_desc *desc,
 void gdsc_unregister(struct gdsc_desc *desc)
 {
 	struct device *dev = desc->dev;
+	struct gdsc **scs = desc->scs;
 	size_t num = desc->num;
+	int i;
 
-	gdsc_pm_subdomain_remove(desc, num);
 	of_genpd_del_provider(dev->of_node);
+	gdsc_pm_subdomain_remove(desc, num);
+
+	for (i = 0; i < num; i++) {
+		if (!scs[i])
+			continue;
+		pm_genpd_remove(&scs[i]->pd);
+	}
 }
 
 /*
