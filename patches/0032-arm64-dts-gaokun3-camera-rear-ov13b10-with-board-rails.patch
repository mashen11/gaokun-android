arm64: dts: gaokun3: 按板级电源表接回后摄 —— 它是 OV13B10，不是 S5K3L6（⚠️ 试验补丁，叠在 0018 之后）

**依据（#106）**：
1. 华为 Windows 驱动包 `qccamrearsensor_extension8280.cab` 里的 `CAMS_RES_QRD.bin`（QRD 平台 = 本机）
   是后摄设备 `\_SB.CAMS` 的电源序列：
   ```
   GPIO7 低（复位）  GPIO92 高（核心轨门控）  GPIO93 低（闪光灯）
   LDO2_B 2.8V   LDO2_C 1.8V   LDO7_B 2.8V（VCM）   延时 1ms
   GPIO7 高  延时 10ms  cam_cc_mclk4_clk 24 MHz
   ```
   同一格式解前摄 `CAMF_RES_QRD.bin`：GPIO15 复位 / LDO11_C 2.8V / LDO2_C 1.8V / GPIO44 高 / MCLK3 24 MHz
   —— 与现役 DT 里 hi846 的接线**逐项一致**，格式解读由此校准。
2. 驱动包里后摄有**两个**模组：`ofilm_ov13b10`（"Makena"）与 `lijing_s5k3l6`（"Makena RFC"）。
   内核 #16 用 S5K3L6 驱动按上面的序列把轨拉起来后**扫整条 CCI 总线**：`0x36`（OV13B10）与 `0x50`
   （模组 EEPROM）应答，`0x10`（S5K3L6）无人 ⇒ **本机后摄是 OV13B10**。
3. L2B 共用：DSDT（`refs/matebook-e-go-linux/docs/acpi/DSDT_216.dsl:3200`）显示设备对 LDO2_B 投 1.8V，
   后摄投 2.8V，RPMh 取最大 ⇒ Windows 下后摄开着时面板 VDDI 在 2.8V。驱动侧对应改动在 `patches/0034`。

**节点**：`ovti,ov13b10` @0x36，`avdd=l2b / dovdd=l2c / dvdd=vreg_camr（GPIO92）`，`reset-gpios` GPIO7 低有效，
MCLK4 **19.2 MHz**（上游 ov13b10 的寄存器表按 19.2 MHz 写，Windows 模组用 24 MHz 是它自己的表），
4 lane，`link-frequencies = 560 MHz`（上游驱动唯一支持的一档）。旧 s5k3l6 节点里 vddio/vddd 是接反的。

⚠️ 仍是试验：0018 的理由（sensor 绑不上会卡死 v4l2-async notifier、前摄跟着没）依然成立，只进测试槽；
后摄 probe 成功（读到 chip id 0x560d42）后再提升。⚠️ diff 由 `git diff` 生成。

diff --git a/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi b/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi
index 690db45..937eda9 100644
--- a/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi
+++ b/arch/arm64/boot/dts/qcom/sc8280xp-huawei-gaokun3-camera.dtsi
@@ -60,6 +60,10 @@
 		compatible = "regulator-fixed";
 
 		regulator-name = "vreg_camr";
+		/* Rear sensor digital core rail, gated by GPIO92 (Windows CAMS_RES_QRD).
+		 * The board LDO sets the real voltage; 1.2V is OV13B10 DVDD nominal. */
+		regulator-min-microvolt = <1200000>;
+		regulator-max-microvolt = <1200000>;
 
 		gpio = <&tlmm 92 GPIO_ACTIVE_HIGH>;
 		enable-active-high;
@@ -122,14 +126,23 @@
 	status = "okay";
 
 	ports {
-		/* port@0 (csiphy0 -> rear s5k3l6) removed: vdda(l2b) is held at
-		 * 1.8V by DSI vddi while S5K3L6 wants 2.8V - sensor NAKs on CCI
-		 * (i2c -6), and v7.2 camss iterates endpoints with
-		 * fwnode_graph_for_each_endpoint (no availability check), so a
-		 * wired-but-never-binding sensor blocks the whole v4l2-async
-		 * notifier - the front camera would get no /dev/v4l-subdev*
-		 * either. Shared-rail conflict needs board-level evidence.
+		/*
+		 * port@0: csiphy0 -> rear OV13B10. Restored 2026-09-14 with the
+		 * board's real power sequence (Windows CAMS_RES_QRD.bin, see
+		 * docs/stage4-findings.md #106): LDO2_B 2.8V (vdda), LDO2_C 1.8V
+		 * (vddio), GPIO92-gated rail (vddd), reset GPIO7, MCLK4 24MHz.
+		 * L2B is shared with the panel's vddi (1.8V vote); the sensor
+		 * driver raises it to 2.8V while powered, exactly like Windows.
 		 */
+		port@0 {
+			csiphy0_ep: endpoint@0 {
+				reg = <0>;
+
+				clock-lanes = <7>;
+				data-lanes = <0 1 2 3>;
+				remote-endpoint = <&ov13b10_ep>;
+			};
+		};
 
 		port@3 {
 			csiphy3_ep: endpoint@0 {
@@ -163,7 +176,46 @@
 	 *
 	 * This sensor has never been detected on 2023 model
 	 */
-	/* camera_rear (s5k3l6xx) removed - see camss port@0 note above */
+	/*
+	 * Rear camera. The board's Windows driver package ships two rear modules
+	 * (ofilm_ov13b10 and lijing_s5k3l6); on this unit a bus scan with the
+	 * rails up (2026-09-14, docs/stage4-findings.md #106) answered at 0x36
+	 * (OV13B10) and 0x50 (module EEPROM), nothing at 0x10 - so it is the
+	 * OV13B10. Rails from CAMS_RES_QRD.bin: LDO2_B 2.8V (avdd, shared with
+	 * the panel's 1.8V vddi - the driver raises it while powered), LDO2_C
+	 * 1.8V (dovdd), GPIO92-gated core rail (dvdd), reset GPIO7, MCLK4.
+	 * The upstream ov13b10 register tables assume a 19.2 MHz MCLK.
+	 */
+	camera_rear: camera@36 {
+		compatible = "ovti,ov13b10";
+		reg = <0x36>;
+
+		pinctrl-0 = <&camr_rgb_default>;
+		pinctrl-names = "default";
+
+		clocks = <&camcc CAMCC_MCLK4_CLK>;
+		assigned-clocks = <&camcc CAMCC_MCLK4_CLK>;
+		assigned-clock-rates = <19200000>;
+
+		reset-gpios = <&tlmm 7 GPIO_ACTIVE_LOW>;
+
+		avdd-supply = <&vreg_l2b>;
+		dovdd-supply = <&vreg_l2c>;
+		dvdd-supply = <&vreg_camr>;
+
+		lens-focus = <&voice_coil_motor>;
+
+		orientation = <1>;
+		rotation = <180>; /* FIXME */
+
+		port {
+			ov13b10_ep: endpoint {
+				data-lanes = <1 2 3 4>;
+				link-frequencies = /bits/ 64 <560000000>;
+				remote-endpoint = <&csiphy0_ep>;
+			};
+		};
+	};
 
 	// eeprom@50/51 <= l2c
 };
