# 2026-09-14 收尾计划：把挂着的问题一次清掉

> 按"一次构建能解决多少"分批，而不是按重要性排 —— 每批一次内核构建 + 一次开机验收，
> 开机要人在场。每一项写清判据；做不到的写清为什么，不假装做了。

## 第一批：一次内核构建（#19）

| # | 项 | 做法 | 判据 |
|---|---|---|---|
| 1 | **camss 容忍没绑上的传感器**（S5K3L6 模组的机器会前后摄一起消失） | camss probe 后起一个延时工作（默认 15 s）：notifier 还没完成 ⇒ 注销、剔掉从未绑上的 fwnode、只带已绑上的重新注册 | 人为让 ov13b10 绑不上（模块参数 `fail_probe`），前摄仍有 `/dev/v4l-subdev*`、camtest 出帧；正常情况 47 个 subdev 不变 |
| 2 | ov13b10 补 `get_selection`（libcamera 警告"驱动要修"） | 照上游 ov13b10 后续版本 / imx 系写法，返回 native/crop 矩形 | libcamera 日志里 `Unable to get rectangle` 消失 |
| 3 | 闪光灯 | `LEDS_CLASS_FLASH`+`LEDS_QCOM_FLASH`=y，pmc8280c 下加 `qcom,pm8350c-flash-led` 节点；**通道未知**，先只配 torch 低电流逐路试 | `/sys/class/leds/` 出现 flash 设备，某一路 torch 亮（用户看） |
| 4 | camtest 重编（退出清零 Test Pattern） | 构建机上编 | 跑完 `v4l2 ctrl` 为 0 |
| 5 | `patches/0022` 健康状态验证 | 新内核开机后 camcc unbind/rebind 一次 | 不 panic、相机恢复 |

## 第二批：用户态 + 一次 ROM 构建（v0.6.1）

| # | 项 | 做法 | 判据 |
|---|---|---|---|
| 6 | 后摄暗处可用 | libcamera：`camera_sensor_helper` 加 ov13b10（增益模型从内核驱动定义推：`ANA_GAIN_MIN 0x80`=1×，线性 code/128）、`camera_sensor_properties` 加 ov13b10、`ov13b10.yaml` | 暗处预览不再近黑；日志无 "Failed to create camera sensor helper" |
| 7 | Updater 清单地址切到 GitHub（释放 R2 桶的前提） | `updater_server_url` overlay → `raw.githubusercontent.com/.../ota/gaokun3.json`；README/INSTALL 同步 | 设备端「系统更新」能取到清单（200） |
| 8 | UBWC 仓库与设备一致（B5b） | 把设备上开着的配置写进仓库 | 构建产物里对应配置为开 |
| 9 | libcamera 生成源码 → Soong genrule | 用 libcamera 自带 Python 生成器 | 干净 checkout 能编 |

## 第三批：需要条件或长期观察（做能做的那一半）

| # | 项 | 能做什么 | 做不到的部分 |
|---|---|---|---|
| 10 | 前摄 CCM | 需要色卡拍摄 | **要用户提供色卡**，否则跳过 |
| 11 | WAN 吞吐 1/20 | 测 MTU / DNS / TCP 窗口 / 5 GHz vs 2.4 / 与手机同 AP 对比 | 若是运营商/路由器侧则记录结论 |
| 12 | 音频/蓝牙死锁 | 检查 hangdump 是否已抓到；没有就只能等 | 无法人为复现 |
| 13 | SELinux enforcing | 把 B6 做掉就能拆 smmustall；hangdump 的 debugfs 改成读 `/proc` | 需要单独一轮 |
| 14 | GPU SMMU 中断 675/680 | DTB 试改 + 用 turnip 制造一次 fault 验证 | 现在 fault=0，制造 fault 有风险，放最后 |
| 15 | 自动亮度 / UCSI / 恢复出厂 / 硬件编码 | 记录现状 | 判死或属另一阶段（B3） |

## 对外（等你点头，我先把稿备好）

16. 上游投稿：`0031`（camcc RCG shared）、`0027`（GDSC 等待值）、`0034`（ov13b10 OF 匹配）—— `git format-patch` 成英文稿放 `docs/upstream/`。
17. PR #3 回复、`drm_crtc` BUG_ON 报 dri-devel、删不删 v0.2.0 的 R2 产物。

## 顺序

1 → 3 → 2 → 4 → 5（一次构建、一次开机）→ 6 → 7 → 8 → 9（一次 ROM）→ 11 → 12 → 13 → 14 → 16/17 备稿。
